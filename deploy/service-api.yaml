apiVersion: v1
kind: Service
metadata:
  name: tinycloud-api
  namespace: tinycloud
spec:
  selector:
    app: tinycloud-api
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP
